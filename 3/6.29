// 6.29
// (Coin Tossing) Write an application that
// simulates coin tossing. Let the program toss a
// coin each time the user chooses the "Toss
// Coin" menu option. Count the number of times
// each side of the coin appears. Display the
// results. The program shoudl call a separate
// method flip that takes no arguments and
// returns a value from a Coin enum (HEADS and
// TAILS)

import java.util.Scanner

public class CoinToss {
	static enum Coin = {TAILS,HEADS}
	static unsigned int headCount = 0, 
		tailCoint = 0;	
	public static void main(String[] args) {
	
		switch(printMenu()) {
		case 1:
		case 2:
		case 3:
		}	
	}
	
	public static int printMenu() {
	
	}
}
